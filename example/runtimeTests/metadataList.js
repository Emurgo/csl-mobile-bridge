// @flow

import {
  MetadataList,
  TransactionMetadatum,
} from '@emurgo/react-native-haskell-shelley'

import {testVector} from '../util'

/**
 * ByronAddress
 */

const test: () => void = async () => {
  throw new Error('TODO')
  // await testVector(MetadataList, TransactionMetadatum, txMetadatumItem)
}

export default test
